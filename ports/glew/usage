glew provides CMake targets:

  find_package(GLEW REQUIRED)
  target_link_libraries(main PRIVATE GLEW::GLEW)

  find_package(GLEW CONFIG REQUIRED)
  target_link_libraries(main PRIVATE GLEW::GLEW)

glew provides the `glew` pkg-config module.
