!!!

left to do:

2. stress test script (overdraw rects, strokes, nineslices, TEXT!!!)
3. smart caching, better LRU (generational, fast path for first gens?)
4. perhaps "[xyz]_direct" variants of functions that immediately construct the required objects instead of requiring an object handle

at the end: d2d shim
